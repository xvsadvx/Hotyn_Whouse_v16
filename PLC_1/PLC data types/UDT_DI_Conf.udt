TYPE "UDT_DI_Conf"
VERSION : 0.1
   STRUCT
      Mode { S7_SetPoint := 'True'} : Int;   // Режим: 0=Прямой, 1=С фильтром (Прямой), 2=С фильтром (Инверсный)
      FilterTime { S7_SetPoint := 'True'} : Int;   // Время антидребезга (мс), макс. 32767 мс
   END_STRUCT;

END_TYPE

